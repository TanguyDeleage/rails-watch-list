<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Submit the list", class: "btn btn-primary" %>
<% end %>

<%= link_to("Back", lists_path) %>
